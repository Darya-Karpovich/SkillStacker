query getUserSkillsWithUserDetails {
    userSkillsWithUserDetails {
      id
      likeValue
      experienceValue
      user {
        id
        name
        surname

      }
      skill {
        id
        name
      }
    }
  }
